# 安卓个人应用项目总纲

## 项目定位

开发一款仅供本人使用、可长期扩展的 Android App。App 将逐步加入不同功能；每个功能独立开发、独立维护，最终在发布版本时统一集成为一个 APK。

## 长期目标

1. 采用模块化开发和模块化维护。
2. 每个功能拥有独立文件夹，功能代码、文档、测试和会话记录均归属自己的模块。
3. 每个功能使用独立的 Codex 开发会话，避免不同功能之间的上下文相互干扰。
4. 开发某个功能时，不读取其他功能模块的内部代码和内部文档。
5. 模块之间不得直接依赖对方的内部实现；确需协作时，只能通过明确、稳定的公共接口或公共契约完成。
6. 版本发布时，由独立的集成与发版流程汇总各模块，统一构建并输出 APK。
7. 每次发布必须记录版本号、发布日期、包含的模块与功能、主要变更、测试结果、APK 文件信息及校验值，确保历史可追溯。
8. 新功能的加入不得破坏已有功能；每个模块应能独立修改、测试和回退。

## 开发阶段

### 第一阶段：单词记忆

建立协助本人记忆英语单词的功能模块。具体需求、交互方式和验收标准将在该模块开始开发时单独确定。

### 后续阶段

功能二及以后功能暂不预设。确定需求后，为其建立新的独立模块和独立开发会话，不与已有功能混合开发。

## 模块与会话边界

- 普通功能开发会话只能读取：项目总纲、公共规范、公共接口，以及当前目标模块内的文件。
- 普通功能开发会话禁止读取其他功能模块的代码、设计文档和会话记录。
- 只有专门的集成与发版会话可以汇总所有模块，但原则上只读取各模块对外公布的接口、状态、测试结果和发布说明。
- 若公共能力需要复用，应提取为独立公共模块，不得复制或暗中依赖某个功能模块的内部实现。

## 版本与历史原则

- 使用统一版本号管理 APK。
- 开发记录与正式发布记录分开保存。
- 已发布版本的记录只追加勘误，不覆盖、不删除。
- 每次发布保留对应的变更说明、模块版本、构建结果和 APK 校验值。
- 未经真实设备安装和核心流程验证，不把版本标记为“设备验证通过”。

## 当前状态

- 项目阶段：第一阶段功能持续开发
- 当前功能：功能 1——语境记忆，v0.1.0 已由用户确认核心流程正确
- Android 工程：已建立，位于 `feature-01-word-memory`
- 当前发布基线：v0.1.0；本地 Git 标签 `feature-01-v0.1.0-user-tested`
- 当前开发目标：v0.2.0，为每个笔记本增加十档学习字号并进行无损数据库迁移
- 功能基线、变更记录和发布证据以目标模块内文档为准

## 本文件的地位

本文件是整个项目的最高级目标说明。后续模块设计、开发规则和发布流程不得与本文件冲突；如需修改长期目标，应先更新本文件，并在未来的项目变更记录中留下原因和日期。
