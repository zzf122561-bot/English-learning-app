# 安卓个人应用项目总纲

## 项目定位

开发一款仅供本人使用、可长期扩展的 Android App。App 将逐步加入不同功能；每个功能独立开发、独立维护，最终在发布版本时统一集成为一个 APK。

## 长期目标

1. 采用模块化开发和模块化维护。
2. 每个功能拥有独立文件夹，功能代码、文档、测试和会话记录均归属自己的模块。
3. 每个功能使用独立的 Codex 开发会话，避免不同功能之间的上下文相互干扰。
4. 开发某个功能时，不读取其他功能模块的内部代码和内部文档。
5. 模块之间不得直接依赖对方的内部实现；确需协作时，只能通过明确、稳定的公共接口或公共契约完成。
6. 版本发布时，由独立的集成与发版流程汇总各模块，统一构建并输出 APK。
7. 每次发布必须记录版本号、发布日期、包含的模块与功能、主要变更、测试结果、APK 文件信息及校验值，确保历史可追溯。
8. 新功能的加入不得破坏已有功能；每个模块应能独立修改、测试和回退。

## 开发阶段

### 第一阶段：单词记忆

建立协助本人记忆英语单词的功能模块。具体需求、交互方式和验收标准将在该模块开始开发时单独确定。

### 第二阶段：内置词典

建立“功能2：词典”模块，支持两个用户私有 MDX/MDD 词典随 APK 内置、完整查词、图片与发音、词典启停和排序，以及后续从 Android 本地导入和删除自定义词典。功能1仅通过公共回调发起临时查词，Android 返回后恢复功能1原页面。

### 后续阶段

功能三及以后功能由用户先指定对话类型和目标，再由总控完成调研、架构决策、模块初始化和任务交接；不得直接在已有功能中混合开发。

## 模块与会话边界

- 普通功能开发会话只能读取：项目总纲、公共规范、公共接口，以及当前目标模块内的文件。
- 普通功能开发会话禁止读取其他功能模块的代码、设计文档和会话记录。
- “总纲”是唯一的总控、集成与发版对话，负责各功能的 Git、合并、App 壳、签名、APK、发布历史和跨对话调度。
- 功能对话只开发 Android Library 模块和运行模块测试，不得自行生成 APK、提交、打标签或发版。
- Skill 对话不进入本仓库或 App，由用户自行管理；总控不读取其内部内容。
- 新对话必须由用户指定类型；未分类前保持 `pending-user-classification`，总控不得自行决定。
- 若公共能力需要复用，应提取为独立公共模块，不得复制或暗中依赖某个功能模块的内部实现。

## 新功能生命周期

1. 用户明确指定新对话为 `app-feature`。
2. 总控调研可行性、锁定产品与架构决策。
3. 总控初始化独立模块、文档、公共契约和 worktree。
4. 总控创建并交接功能任务；功能任务负责后续实现、升级和模块测试。
5. 功能任务在版本完成、契约或数据库变化、测试结果、阻塞及验收反馈时主动向总控汇报。
6. 总控独占差异审查、Git 提交与合并、App 壳集成、版本、签名、APK 和发版记录。

## 版本与历史原则

- 使用统一版本号管理 APK。
- 开发记录与正式发布记录分开保存。
- 已发布版本的记录只追加勘误，不覆盖、不删除。
- 每次发布保留对应的变更说明、模块版本、构建结果和 APK 校验值。
- 未经真实设备安装和核心流程验证，不把版本标记为“设备验证通过”。

## 当前状态

- 项目阶段：v0.3.0 test.001 已从干净提交构建、签名并归档；等待用户自行实机覆盖安装验收
- 当前功能：功能 1——语境记忆，v0.2.0 已由用户确认可用、无问题；v0.3.0 新增长按普通字符串查词回调
- 新增功能：功能 2——词典，模块版本 v0.1.0；纯 Kotlin/JVM MDX/MDD 内核、查询、管理、导入、安全展示与发音回退已完成自动验证
- 当前发布基线：v0.2.0；代码标签 `feature-01-v0.2.0-font-sizing`，验收标签 `feature-01-v0.2.0-user-verified` 与 `feature-01-v0.2.0-user-tested`
- Git：根仓库由总控独占管理；功能对话使用独立 worktree，不拥有提交或标签权限
- Android：根 App v0.3.0 / versionCode 4 使用“记单词 / 词典”双入口；两套功能均为 Android Library，两套私有词典只在构建时校验并打入忽略目录
- 设备边界：当前没有连接 Android 设备；覆盖安装、长按手势、导航、WebView、SAF、媒体与 TTS 由用户收到测试 APK 后自行实机验收
- 发布证据：以 `_manager/releases` 为准；功能行为基线仍以目标功能目录为准

## 本文件的地位

本文件是整个项目的最高级目标说明。后续模块设计、开发规则和发布流程不得与本文件冲突；如需修改长期目标，应先更新本文件，并在未来的项目变更记录中留下原因和日期。
