# EnglishApp v0.3.0 test.002 真机验收单

## 候选产物锁定

- 文件：`EnglishApp-v0.3.0-test.002-6010e73.apk`
- SHA-256：`C0D02FCB78051626EEC53C5CF64CEA41B06B2DC17E281ABB51EA511DF3FE64B7`
- 包名：`com.xuesui.englishapp`
- 安装后版本：`0.3.0` / versionCode `4`
- 强制要求：直接覆盖当前已安装版本；不得先卸载，不得清除应用数据。

文件名或 SHA-256 不一致时停止验收，不要用其他 APK 替代。若系统拒绝同版本覆盖安装，请保留现有 App 和数据，只回报系统原始提示，不要卸载。

## 2026-08-24 用户实机反馈

- 执行者：用户自行实机测试；总控未连接或操作Android设备。
- 用户结论：除“词典具体内容页面的链接被阻止”和“无法获得内部发音”外，其余均正常，验收通过；两个问题先记录、不修复。
- 验收级别：`用户验收通过（接受已知问题）`。
- 已知问题：`F02-LINK-001`、`F02-AUDIO-001`，记录于 `_manager/features/feature-02/KNOWN_ISSUES.md`。
- 证据限制：未提供设备型号、Android版本、逐项截图或日志；下表“通过”均表示用户本次概括确认，不表示总控亲自执行。

## A. test.001 缺陷优先复测

| 核对项 | 预期结果 | 用户结果 |
|---|---|---|
| 原查询复现 | 搜索 `another place` 后 Collins 与 Oxford 标签均可出现 | 用户概括确认通过 |
| Collins 正文 | 点击 Collins 后显示真实词条正文，不再出现 `data:text/html...` 或 `ERR_HTTP_RESPONSE_CODE_FAILURE` | 用户概括确认通过 |
| Oxford 正文 | 切换 Oxford 后显示真实词条正文，不再出现原错误 | 用户概括确认通过 |
| CSS 与图片 | 词条排版生效；含图片词条能显示本地 MDD 图片 | 用户概括确认通过 |
| 安全边界 | 普通词条和本地资源可用，外部页面不会在词条 WebView 内打开 | 正文与资源正常；具体内容链接被阻止，登记为 `F02-LINK-001` |

## B. 覆盖安装与功能1基线

| 核对项 | 预期结果 | 用户结果 |
|---|---|---|
| 覆盖安装 | 不卸载旧版即可安装并启动 | 用户概括确认通过 |
| 旧笔记本和学习位置 | 名称、内容、顺序、位置及字号全部保留 | 用户概括确认通过 |
| 功能1基线 | 中文显隐、目标强调、默写、滚动和字号行为不变 | 用户概括确认通过 |
| 默认入口与双入口 | 启动仍进入记单词；底部可切换“记单词 / 词典” | 用户概括确认通过 |

## C. 快速查词与导航

| 核对项 | 预期结果 | 用户结果 |
|---|---|---|
| 英文长按 | 长按英文正文单词，打开对应 QUICK_LOOKUP 页面 | 用户概括确认通过 |
| 边界字符 | 词内撇号/连字符可包含；中文、空白、数字和纯标点不触发 | 用户概括确认通过 |
| 一次返回 | 系统返回或页面返回一次即回到原笔记本和原位置 | 用户概括确认通过 |
| 内部链接 | 点击词条内部词汇只替换当前结果，不增加返回层级 | 未通过：具体内容页面链接被阻止；`F02-LINK-001`，用户接受延期 |

## D. 词典功能

| 核对项 | 预期结果 | 用户结果 |
|---|---|---|
| 两本内置词典 | Collins 与 Oxford 均可查询，顺序与管理页一致 | 用户概括确认通过 |
| 发音 | 点击发音可用；无 MDD 音频时回退 Android 英语 TTS | 部分未通过：无法获得内部发音；`F02-AUDIO-001`，用户接受延期 |
| 启停与排序 | 状态和顺序重启后保留，查询标签同步 | 用户概括确认通过 |
| 内置删除边界 | 两本内置词典不可删除 | 用户概括确认通过 |
| 自定义导入/删除 | 有额外 MDX/MDD 时验证导入与私有副本删除；没有文件则回报“未执行” | 用户概括确认其余正常；未单独提供导入步骤证据 |

## E. 用户回报格式

```text
[APP_ACCEPTANCE]
App 版本：0.3.0 test.002
设备型号 / Android 版本：
覆盖安装：成功 / 失败（若失败附系统原始提示）
another place 两本正文：通过 / 失败
CSS / 图片：通过 / 失败
旧数据与功能1基线：通过 / 失败
双入口与快速查词返回：通过 / 失败
发音、管理与导入：通过 / 失败 / 新词典导入未执行
问题说明：无 / 具体描述
用户结论：确认通过 / 不通过
```

## F. 正式提升门槛

用户已概括确认除两个登记问题外其余验收通过，并明确接受延期修复。总控据此沿用本页锁定的同一APK提升v0.3.0并创建 `app-v0.3.0` 标签；不重新构建不同二进制。
