# EnglishApp v0.3.0 test.001 真机验收单

## 候选产物锁定

- 文件：`EnglishApp-v0.3.0-test.001-bfbfcc5.apk`
- SHA-256：`03E1863D2BBE44BD224187A09A0D0093A12F567072C6F4FFBEE402650F1D0E35`
- 包名：`com.xuesui.englishapp`
- 安装后版本：`0.3.0` / versionCode `4`
- 强制要求：直接覆盖当前已安装版本；不得先卸载，不得清除应用数据。

文件名或 SHA-256 不一致时停止验收，不要用其他 APK 替代。

## 2026-08-24 用户实机反馈

- 证据：用户提供词典界面截图。
- 已确认：App能运行、可进入词典、搜索 `another place`，并显示Collins与Oxford结果标签。
- 失败：词条正文区域显示 `data:text/html;charset=utf-8;base64,` 加载失败，错误 `net::ERR_HTTP_RESPONSE_CODE_FAILURE`。
- 结论：`test.001` 用户验收不通过，以下未确认项目保持待验收，不得据此推断通过。

## A. 覆盖安装与旧数据

| 核对项 | 预期结果 | 用户结果 |
|---|---|---|
| 覆盖安装 | 不卸载旧版即可安装并启动 | 待确认 |
| 旧笔记本 | 名称、内容和顺序全部保留 | 待确认 |
| 学习位置 | 打开旧笔记本仍回到原位置 | 待确认 |
| 中文显隐 / 目标强调 / 默写 | v0.2.0 已验收行为不变 | 待确认 |
| 每笔记本字号 | 原字号保持，十档调节仍可用 | 待确认 |

## B. App 导航与快速查词

| 核对项 | 预期结果 | 用户结果 |
|---|---|---|
| 默认入口 | 启动后仍直接显示“记单词” | 待确认 |
| 底部入口 | 可在“记单词 / 词典”之间切换 | 待确认 |
| 英文长按 | 长按英文正文中的单词，直接打开该词的临时查词页 | 待确认 |
| 边界字符 | 撇号或连字符位于词内时包含；长按中文、空白、数字或标点不触发 | 待确认 |
| 一次返回 | 系统返回或页面返回一次即回到功能1原笔记本和原位置 | 待确认 |
| 连续查询 | 先后长按两个不同单词，第二次显示最新单词，不复用旧结果 | 待确认 |

## C. 内置词典与展示

| 核对项 | 预期结果 | 用户结果 |
|---|---|---|
| 首次准备 | 首次进入词典会复制并校验两套内置词典，完成后可查询 | 待确认 |
| 两个标签 | Collins 与 Oxford 均出现，顺序与管理页一致 | 待确认 |
| 精确查询 | Collins 可查 `Zulu`，Oxford 可查 `Frisian` | 待确认 |
| 前缀建议 | 输入不存在的前缀时可显示去重建议 | 待确认 |
| 内部链接 | 点击词条内部词汇只替换当前结果，不增加返回层级 | 待确认 |
| CSS 与图片 | 两套词条样式正常，包含图片的词条可显示图片 | 待确认 |
| 发音 | 点击发音可用；当前两套MDD无音频时应回退 Android 英语 TTS | 待确认 |

## D. 词典管理与导入

| 核对项 | 预期结果 | 用户结果 |
|---|---|---|
| 启用 / 停用 | 停用后不参与查询，重启后状态保留 | 待确认 |
| 排序 | 拖动或箭头排序，查询标签顺序同步且重启保留 | 待确认 |
| 内置删除 | 两本内置词典没有删除按钮 | 待确认 |
| 重复导入 | 选择与内置词典相同的文件夹会拒绝重复且不留半成品 | 待确认 |
| 新词典导入 | 若有另一套MDX/MDD测试词典，可从文件夹导入并查询；没有测试文件时明确回报“未执行” | 待确认 |
| 导入词典删除 | 只删除App私有副本，不修改用户选择的原文件 | 待确认 |

## E. 用户回报格式

```text
[APP_ACCEPTANCE]
App 版本：0.3.0 test.001
设备型号 / Android 版本：
覆盖安装：成功 / 失败
旧数据保留：通过 / 失败
功能1基线：通过 / 失败
双入口与快速查词：通过 / 失败
内置词典查询/图片/发音：通过 / 失败
管理与导入：通过 / 失败 / 新词典导入未执行
问题说明：无 / 具体描述
用户结论：确认通过 / 不通过
```

## F. 正式提升门槛

只有覆盖安装、受保护旧数据、核心学习流程、双入口、快速查词返回和两套内置词典核心查询均获得用户真实反馈后，才能更新本清单并创建 `app-v0.3.0` 标签。正式提升必须沿用本页锁定的同一 APK，不重新构建其他二进制。
