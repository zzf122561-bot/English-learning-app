# Codex EnglishApp 总控维护规则

## 对话类型

- `manager`：总纲对话，独占 Git 提交、合并、标签、App 版本、签名、APK 和正式发版权限。
- `app-feature`：由用户明确指定的 App 功能对话，只维护自己的功能模块。
- `github-sync`：由用户明确指定的 GitHub 同步对话，只能在总控转发用户明确授权后，把已经存在的本地提交或标签普通推送到登记远端；不得开发、编辑、提交、合并或改写历史。
- `skill`：由用户自行管理，不进入本仓库、不进入 App、总控不读取其内部文件。
- `pending-user-classification`：新对话的默认状态。只有用户可以改变类型；总控不得自行判断。

类型与任务 ID 以 `_manager/THREAD_REGISTRY.md` 为准。

## 新功能创建与交接

- 新功能必须先由用户明确指定为 `app-feature`，再由总控完成调研、决策、模块初始化和独立 worktree 任务创建。
- 新功能任务创建后，日常实现、升级和模块测试由对应功能对话负责；总控只做规则维护、集成审查和发版。
- 功能任务必须按 `_manager/REPORTING_PROTOCOL.md` 主动汇报里程碑，不得等待总控轮询后才补写状态。
- 用户私有测试资料只能通过总控列出的精确路径授权；不得编辑、移动、上传或提交 Git。

## 功能对话修改前必读

1. 读取根目录 `PROJECT.md`。
2. 只进入本次目标功能目录，读取其中的 `AGENTS.md`、`FEATURE_BASELINE.md`、`STATUS.md` 和 `CHANGELOG.md`。
3. 不得读取其他功能目录、`_manager` 或 Skill 目录；只有总控消息明确列出的单个文件例外。
4. 不得读取全局或项目外的 memory、历史摘要、其他对话缓存；需要上下文时只能向总控汇报并请求精确授权路径。

## 权限边界

- 功能对话不得执行 Git 提交、合并、标签或历史改写。
- 功能对话不得修改根 App 壳、全局 Gradle 配置、版本号、签名、发布记录或生成 APK。
- 功能模块必须使用 Android Library 插件，不得声明 launcher Activity 或依赖其他功能内部实现。
- 未经用户明确许可，不得删除、替换或弱化已经写入功能基线的行为。
- 数据库只允许显式、可测试的向前迁移；禁止 destructive migration、删库重建和清空用户历史。
- 用户文档、个人数据库、签名密钥、APK、恢复包和构建缓存不得提交 Git。

## 本地 Git 与 GitHub 同步

- 本地仓库是开发与历史记录的主来源；所有项目修改必须先由总控检查并提交到本地 Git。
- GitHub 登记远端固定为 `https://github.com/zzf122561-bot/English-learning-app.git`。
- 没有用户针对本次同步的明确指令时，总控不得要求同步对话 push，同步对话也不得自动 fetch、pull、push 或定时同步。
- 用户明确要求更新 GitHub 后，由总控向 `github-sync` 对话下发精确的目标分支、提交或标签；默认仅允许普通、非强制、逐引用推送。
- 禁止 `--force`、`--force-with-lease`、`--mirror`、`--all`、删除远端引用或覆盖历史；不能快进时必须停止并回报总控。
- Git 忽略的 APK、词典原文件、签名密钥、本地配置、数据库、缓存和用户私有资料不得上传。
- 每次同步必须回报远端 URL、推送引用、本地与远端提交 SHA 和结果；GitHub 凭据不得读取、记录或输出。
- 详细流程以 `_manager/GITHUB_SYNC_POLICY.md` 为准。

## 总控完成标准

- 读取功能汇报，检查差异并复跑目标模块原有测试。
- 总控批准某一里程碑后，以当时差异和测试证据建立审查锚点；后续只审查锚点之后新增或修改的代码，不对未变化代码反复逐行审查。
- 增量审查仍须按风险复跑相关回归；若公共契约、数据库、安全边界或受保护基线被修改，只复审受影响差异及其调用链。
- 运行 `_manager/tools/verify-boundaries.ps1`，确认全项目只有一个 Application 模块。
- 从干净提交构建签名 APK，保存文件名、大小、SHA-256、证书指纹和验收边界。
- 自动测试、设备测试和用户确认分开记录；没有真实证据不得提高验收级别。
- 正式变更由总控提交和打标签，确保可以比较与回退。
