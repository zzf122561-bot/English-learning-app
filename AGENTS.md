# Codex EnglishApp 维护规则

## 修改前必读

1. 读取根目录 `PROJECT.md`。
2. 只进入本次目标功能文件夹，并读取该文件夹的 `AGENTS.md`、`FEATURE_BASELINE.md`、`STATUS.md` 和 `CHANGELOG.md`。
3. 普通功能开发不得读取其他功能目录的内部代码、文档或数据。

## 保护边界

- 未经用户明确许可，不得删除、替换、弱化已经写入功能基线的行为。
- 不得通过重建项目、删除数据库、破坏性迁移或清空历史来解决兼容问题。
- 修改必须局限在目标模块；跨模块能力只能通过明确的公共契约接入。
- 用户文档、个人数据库、签名密钥、APK 和构建缓存不得提交到 Git。

## 完成标准

- 代码改动必须有相应测试，并复跑目标模块原有测试和构建。
- 数据库变更必须有显式迁移和迁移测试，不允许 destructive migration。
- 更新目标模块的 `CHANGELOG.md`、`STATUS.md` 和发布记录。
- 自动测试、设备测试和用户确认必须分开记录；没有真实证据不得提高验收级别。
- 正式变更应形成独立 Git 提交和版本标签，确保可以比较与回退。

